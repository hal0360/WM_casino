<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tw.com.lixin.wm_casino">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:allowNativeHeapPointerTagging="false"
        tools:targetApi="m">
        <activity android:name=".GoldenFlowerActivity"></activity>
        <activity android:name=".AndarBaharActivity" />
        <activity android:name=".FishPrawnActivity" />
        <activity android:name=".FantanActivity" />
        <activity android:name=".SamgongActivity" />
        <activity android:name=".NiuNiuActivity" />
        <activity android:name=".SicBoActivity" />
        <activity android:name=".RouletteActivity" />
        <activity android:name=".DragonTigerActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LobbyActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".BaccaratActivity" />
    </application>

</manifest>